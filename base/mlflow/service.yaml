apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: cinerankml
  labels:
    app.kubernetes.io/name: mlflow
    app.kubernetes.io/part-of: cinerankml
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mlflow
  ports:
    - name: http
      port: 5000
      targetPort: http
