# Placeholder Secret — replace values on the VPS before / after apply.
# Do not commit real production credentials.
apiVersion: v1
kind: Secret
metadata:
  name: cinerankml-secret
  namespace: cinerankml
type: Opaque
stringData:
  POSTGRES_PASSWORD: CHANGE_ME
  DATABASE_URL: postgresql+psycopg://cinerankml:CHANGE_ME@postgres:5432/cinerankml
  S3_ACCESS_KEY: minioadmin
  S3_SECRET_KEY: minioadmin
